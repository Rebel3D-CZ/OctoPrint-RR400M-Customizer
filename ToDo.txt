Vycházím z tohoto pluginu, kde jsou hlavní úpravy zde https://github.com/Rebel3D-CZ/OctoPrint-DetailedProgress-RR400M/blob/master/octoprint_detailedprogressrr400m/__init__.py


**Funkce řádek 28:**

    if event == Events.PRINT_STARTED:
      self._printer.commands("M118 A1 P0 action:print_start")
    
**Funkce řádek 40:**

    elif event in (Events.PRINT_DONE, Events.PRINT_FAILED, Events.PRINT_CANCELLED):
      self._printer.commands("M118 A1 P0 action:print_end")

HOTOVO Funkce řádek 50:

    elif event == Events.CONNECTED and self._settings.get(["show_ip_at_startup"]):
		  self._printer.commands("M118 A1 P0 action:notification IP: {}".format(ip))
    
**Funkce řádek 55:**

    elif event == Events.PRINT_PAUSED:
			self._printer.commands("M118 A1 P0 action:pause")
   
**Funkce řádek 61:**

    elif event == Events.PRINT_RESUMED:
			self._printer.commands("M118 A1 P0 action:resume")
      
**Funkce řádek 89:**

    def _update_progress(self, currentData):
			self._printer.commands("M118 A1 P0 action:notification Data Left {}/100".format(progressPerc))
   


    
